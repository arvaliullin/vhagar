version: '3.8'

services:
  rabbitmq:
    image: registry.astralinux.ru/library/astra/ubi18-rabbitmq310:1.8.4
    container_name: vhagar-rabbitmq
    hostname: rabbitmq
    ports:
      - "5672:5672"   # AMQP port
      - "15672:15672" # Management UI port
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - vhagar-network

  producer:
    build:
      context: ..
      dockerfile: build/producer.Dockerfile
    container_name: vhagar-producer
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      AMQP_URL: amqp://guest:guest@rabbitmq:5672/
    networks:
      - vhagar-network
    restart: unless-stopped

  consumer:
    build:
      context: ..
      dockerfile: build/consumer.Dockerfile
    container_name: vhagar-consumer
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      AMQP_URL: amqp://guest:guest@rabbitmq:5672/
    networks:
      - vhagar-network
    restart: unless-stopped

  postgresql11:
    image: registry.astralinux.ru/library/astra/ubi17-postgresql11:1.7.8
    container_name: vhagar-postgresql11
    hostname: postgresql11
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    volumes:
      - postgresql11_data:/var/lib/pgsql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - vhagar-network

volumes:
  rabbitmq_data:
    driver: local
  postgresql11_data:
    driver: local

networks:
  vhagar-network:
    driver: bridge

